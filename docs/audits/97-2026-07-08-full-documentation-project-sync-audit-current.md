# Полный Аудит Документации И Синхронизации Проекта 2026-07-08 После WebMCP

Дата аудита: 2026-07-08.

Этот документ фиксирует текущую полную проверку всей проектной документации после добавления профильного аудита `96` по WebMCP, `llms.txt` и PageSpeed Agentic Browsing. Он заменяет аудит `95` как актуальную точку отсчета для документационного слоя. Аудиты `78`, `82`, `86`, `90`, `91`, `94` и `95` остаются историческими снимками.

## 1. Итог Для Новичка

Документация проекта пригодна для последовательного изучения новичком: она русскоязычная, структурированная, объясняет смысл правил и синхронизирована с текущим состоянием репозитория на 2026-07-08.

После проверки:

- документационный слой содержит **97** нумерованных Markdown-документов `01-97`;
- три сопутствующих CSV-файла остаются рядом с документами `53` и `59`, поэтому физически внутри `docs/` находится **100** документационных файлов;
- `README.md`, `AGENTS.md` и [карта документации](../01-documentation-map.md) ведут к текущему полному аудиту `97`;
- аудит `92` остается профильным UX/UI-снимком;
- аудит `93` остается профильным tooling-снимком перехода на `Hugo 0.164.0`;
- аудит `96` остается профильным WebMCP / `llms.txt` / PageSpeed Agentic Browsing snapshot;
- текущий порядок чтения для новичка сохранен и расширен без разрыва нумерации;
- английский сохранен только там, где это название технологии, команды, файла, поля, метрики, SEO/schema/CWV/WebMCP-термина или официального источника;
- правила `image` + `cover`, schema.org, Entity Registry, review-системы, product facts, Core Web Vitals, PageSpeed Insights, Netlify routing, WebMCP, `llms.txt` и внутренней перелинковки совпадают с текущими файлами проекта.

Итоговая оценка документации: **9.8/10**.

Оценка не `10/10`, потому что часть ограничений относится не к качеству документации, а к текущему состоянию продукта и инфраструктуры: опубликованный Netlify build все еще работает в `development`-режиме, `12` главных товарных PNG требуют фактической замены, данные Google Search Console еще не импортированы, в рабочем дереве есть `.DS_Store`, WebMCP остается экспериментальным Chrome-слоем, а обновления Tailwind CSS и Netlify Database SDK доступны только как отдельная controlled upgrade-задача.

## 2. Что Проверено

Проверены:

- `README.md` и `AGENTS.md`;
- все Markdown-документы внутри `docs/`;
- сопутствующие CSV-файлы документации;
- карта чтения и глобальная нумерация `01-97`;
- русскоязычность активных документов, заголовки, даты и пояснения для новичка;
- локальные Markdown-ссылки внутри документации;
- `content/`, `layouts/`, `assets/`, `static/`, `data/`, `scripts/`;
- `hugo.yaml`, `netlify.toml`, `mise.toml`, `package.json`, `package-lock.json`;
- правила `schema_types`, Entity Registry, product facts, review-системы, image pipeline, внутренней перелинковки, WebMCP, `llms.txt` и production gate;
- официальные практики Hugo, Google Search Central, Google Article structured data, Google Image SEO, Chrome WebMCP, `llms.txt`, AI-контента и Core Web Vitals на дату аудита.

## 3. Найденные Дефекты До Исправления

### P1. После Профильного Документа `96` Полный Audit Snapshot Оставался На `95`

Документ `96` добавил новый профильный слой: WebMCP-аннотации форм, `static/llms.txt` и PageSpeed Agentic Browsing. `README.md`, `AGENTS.md` и `docs/01-documentation-map.md` корректно объясняли, что `96` не заменяет полный аудит `95`, но для запроса “вся документация синхронизирована со всеми изменениями на данный момент” нужен новый полный snapshot после `96`.

Исправление:

- создан текущий полный аудит `97`;
- `README.md`, `AGENTS.md` и `docs/01-documentation-map.md` переключены на `97`;
- аудит `95` помечен как исторический snapshot;
- карта документации обновлена до диапазона `01-97` и `100` физических файлов внутри `docs/`.

### P2. README Указывал Порядок Чтения `01-96`

После добавления нового полного аудита текущий диапазон должен быть `01-97`.

Исправление: раздел “Карта документации” в README обновлен до `01-97`.

### P2. Карта Документации Ссылалась На `95` Как На Текущий Полный Audit

После добавления `97` маршрут “Для проверки или обновления всей документации” должен вести на новый полный snapshot.

Исправление: маршрут переключен на `97`, а `95` помечен как исторический полный аудит.

## 4. Текущее Фактическое Состояние

| Объект | Результат |
|---|---:|
| Markdown-документы в `docs/` после этого аудита | **97** |
| Сопутствующие CSV-файлы в `docs/` | **3** |
| Всего документационных файлов внутри `docs/` | **100** |
| Markdown-страницы контента в `content/` | **100** |
| Локальные файлы `layouts/` | **95** |
| Helper-скрипты в `scripts/` | **12** |
| Static-файлы в `static/` | **18** |
| WebP-файлы внутри `content/` | **154** |
| PNG-файлы внутри `content/` | **14** |
| Product Markdown-файлы | **32** |
| Главные товарные изображения `01-front.*` | **12** |
| Product pages с `review_target_id` | **24** |
| Product pages с `reviews_enabled: true` | **24** |
| Файлы `content/**/*.md` с markdown `# H1` | **0** |
| Файлы `content/**/*.md` с inline-code в видимом контенте | **0** |
| Content files с `schema_types` | **100** |
| Content files со старым `schema_type` | **0** |
| Content files с полным `image` + `cover`-блоком | **100** |
| `static/llms.txt` | **есть** |

Примечание: в рабочем дереве найдены `.DS_Store` файлы: корневой `.DS_Store`, `content/.DS_Store`, `content/products/.DS_Store`, `content/products/xtal/.DS_Store`. Это project hygiene-задача, а не дефект документации; удаление не выполнялось, потому что текущая задача была про документацию, а не про cleanup файлов.

## 5. Сверка Со Стеком

Текущий стек подтвержден файлами проекта и локальными командами:

| Компонент | Где Зафиксирован | Текущее Значение |
|---|---|---|
| `Hugo` | `mise.toml`, `netlify.toml`, README, AGENTS | **0.164.0** |
| `Node` | `mise.toml`, `netlify.toml`, README, AGENTS | **24.16.0** |
| `npm` | локальный runtime через `mise exec` | **11.13.0** |
| `Tailwind CSS` | `package.json`, `package-lock.json` | **4.3.0** |
| `@tailwindcss/cli` | `package.json`, `package-lock.json` | **4.3.0** |
| `@netlify/database` | `package.json`, `package-lock.json` | **1.0.0** |
| Netlify environment | `netlify.toml` | **development** |
| Тема | `hugo.yaml`, AGENTS | `themes/PaperMod` |

Фактические проверки версий:

- `mise current`: `hugo 0.164.0`, `node 24.16.0`;
- `mise exec -- hugo version`: `v0.164.0-ce2470e7012b5ab5fc4e10ebe4027e9f8d9e00dc`;
- `mise exec -- node -v`: `v24.16.0`;
- `npm ls tailwindcss @tailwindcss/cli @netlify/database --depth=0`: все три пакета установлены в зафиксированных версиях;
- `git ls-remote --tags https://github.com/gohugoio/hugo.git 'refs/tags/v0.164.*'`: найден `refs/tags/v0.164.0`.

`npm outdated --json` показывает, что pinned/wanted версии совпадают с lock-файлом, но upstream уже содержит:

- `tailwindcss`: latest `4.3.2`;
- `@tailwindcss/cli`: latest `4.3.2`;
- `@netlify/database`: latest `1.1.0`.

Это не ошибка документации: проект воспроизводимо закреплен на текущем стеке. Обновление npm-пакетов нужно делать отдельной задачей с повторной сборкой, визуальной проверкой и SEO/schema/WebMCP-контролем.

## 6. Сверка С Лучшими Практиками 2026

Документация согласована с актуальными практиками:

- официальный Hugo-документ `css.TailwindCSS` подтверждает, что функция обрабатывает CSS через Tailwind CSS CLI, а начиная с Hugo `0.161.0` standalone Tailwind binary не поддерживается; значит, текущий стандарт “держать `tailwindcss` и `@tailwindcss/cli` в npm-зависимостях проекта” верен для `Hugo 0.164.0`;
- релиз Hugo `0.164.0` опубликован как latest release на GitHub; локальный project pin совпадает с upstream tag `v0.164.0`;
- Google Article structured data требует, чтобы изображения были доступны для обхода, представляли размеченный контент, имели поддерживаемый формат и для лучшего результата покрывали соотношения **16:9**, **4:3**, **1:1** с минимумом **50K** пикселей; это зафиксировано в документах `05`, `06`, `67`, `79`, `89`;
- Google Image SEO требует содержательные изображения, описательные `alt`, корректные HTML-элементы изображений и доступность URL для сканирования; локальный `seo-image`, `cover`-контракт и image playbook этому соответствуют;
- Chrome WebMCP описан как proposed web standard и progressive enhancement для AI-агентов; документация проекта корректно держит его как экспериментальный слой, а не как замену schema.org, accessibility, sitemap или видимого контента;
- Chrome WebMCP Declarative API поддерживает аннотации HTML-форм; проект применяет их к контактной форме, форме отзывов и фильтрам каталога;
- `llms.txt` используется как краткая Markdown-карта для AI/LLM-агентов; документация проекта не превращает его во второй sitemap и не хранит там товарные цены, наличие или guarantee facts;
- web.dev фиксирует текущие Core Web Vitals как `LCP`, `INP`, `CLS` с ориентирами `2.5s`, `200ms`, `0.1` на p75; это закрывают документы `12`, `13`, `14`, `83`;
- Google guidance по AI-generated content делает акцент на точности, качестве, релевантности, metadata, alt и structured data validation; локальные документы `24`, `25`, `26`, `79`, `85`, `88`, `96` усиливают сущности, факты, видимый текст, agentic hints и проверяемую разметку, а не обещают искусственную “AI-видимость”.

## 7. Проверка Русского Языка И Структуры

Активные документы соответствуют стандарту:

- есть H1 и дата `Обновлено:` или `Дата аудита:`;
- заголовки и пояснения написаны по-русски;
- английские слова оставлены только как технические имена, значения полей, команды, метрики или официальные названия;
- длинные документы дают новичку маршрут чтения, смысл документа и проверочные действия;
- исторические audit-файлы не переписываются задним числом, но получают статус, если могут ввести в заблуждение;
- текущие onboarding-документы ведут к актуальным документам, а не к старым снимкам.

Намеренные исключения:

- `README.md`, `AGENTS.md`, имена файлов, команды, поля front matter, schema.org-типы, WebMCP-атрибуты и метрики `LCP` / `INP` / `CLS` не переводятся;
- CSV-значения и технические идентификаторы не русифицируются;
- placeholder-ссылки в документах-шаблонах `10` и `11` являются полями для заполнения, а не реальными ссылками проекта.

## 8. Проверка Ссылок И Нумерации

После исправлений:

- диапазон нумерации внутри `docs/` — **01-97**;
- пропусков и дублей номеров нет;
- все Markdown-документы есть в [карте документации](../01-documentation-map.md);
- все Markdown-документы перечислены в `AGENTS.md`;
- текущий полный аудит документации указывает на `97`;
- реальные локальные Markdown-ссылки не имеют битых целей;
- найдено `11` placeholder-ссылок, все находятся только в документах-шаблонах `10` и `11` и являются ожидаемыми заготовками.

## 9. Фактические Проверки Этого Прогона

| Проверка | Результат |
|---|---|
| Структурный скан документации до добавления `97` | `96` Markdown-документов, `3` CSV, диапазон `01-96`, пропусков и дублей нет. |
| Скан локальных Markdown-ссылок до добавления `97` | Проверено **1 058** repo-ссылок; **0** битых repo-ссылок; **8** site-root ссылок пропущены как URL сайта; **11** placeholder-ссылок только в шаблонах `10` и `11`. |
| Структурный скан документации после добавления `97` | `97` Markdown-документов, `3` CSV, диапазон `01-97`, пропусков и дублей нет; все документы есть в карте и `AGENTS.md`. |
| Скан локальных Markdown-ссылок после добавления `97` | Проверено **1 063** repo-ссылки; **0** битых repo-ссылок; **8** site-root ссылок пропущены как URL сайта; **11** placeholder-ссылок только в шаблонах `10` и `11`. |
| `mise current` | `hugo 0.164.0`, `node 24.16.0`. |
| `mise exec -- hugo version` | `v0.164.0-ce2470e7012b5ab5fc4e10ebe4027e9f8d9e00dc`. |
| `mise exec -- node -v` | `v24.16.0`. |
| `npm ls tailwindcss @tailwindcss/cli @netlify/database --depth=0` | `tailwindcss 4.3.0`, `@tailwindcss/cli 4.3.0`, `@netlify/database 1.0.0`. |
| `npm outdated --json` | pinned/wanted версии совпадают; доступны latest `tailwindcss 4.3.2`, `@tailwindcss/cli 4.3.2`, `@netlify/database 1.1.0`. |
| `git ls-remote` по Hugo `v0.164.*` | Найден `refs/tags/v0.164.0`. |
| `git diff --check` | Пройдено, пробельных ошибок нет. |
| `mise exec -- ./scripts/script_check.sh` | Пройдено на Hugo `0.164.0`. Development build успешен; базовые проверки `_redirects`, `.DS_Store` в `static/` и `public/`, markdown `# H1`, inline-code в `content/`, `schema_type` и noindex служебных страниц прошли. |
| `mise exec -- npm run build:production` | Пройдено на Hugo `0.164.0`. Production build успешен. |
| Скан JSON-LD в готовом HTML | **96** JSON-LD блоков, **0** parse-ошибок. |
| Скан ссылок в готовом HTML | **7 086** внутренних ссылок, **588** внешних ссылок, **1 494** fragment links, **0** пустых `href`, **0** `javascript:`-ссылок, **0** битых внутренних ссылок. |
| Canonical и `hreflang` scan | **129** HTML-страниц с canonical, **232** `hreflang` links. |
| `public/llms.txt` | Файл существует после сборки. |

Локальная сборка выводит `Netlify Database is not available; writing empty reviews snapshot.` Это ожидаемо для локальной среды без подключенной Netlify Database и не является дефектом документации.

## 10. Что Не Удалялось

Legacy-аудиты не удалялись. Причина: они являются audit trail проекта и часто нужны, чтобы понять, почему была принята конкретная SEO/schema/tooling/WebMCP-логика.

Без отдельного подтверждения нельзя удалять:

- исторические audit-файлы `29-96`;
- документы с прошлым target-стеком, если они явно помечены как исторические;
- `.DS_Store` в рабочем дереве, потому что текущая задача была документационной, а не cleanup-задачей.

Что можно удалить или архивировать только после отдельного решения:

- исторические аудиты, если будет выбран отдельный архивный формат;
- `.DS_Store` в корне и `content/`;
- устаревшие скрипты или поля, если они больше не используются и это подтверждено поиском по проекту.

## 11. Оставшиеся Риски

| Приоритет | Риск | Почему Это Не Дефект Документации |
|---|---|---|
| P0 | Netlify опубликованного сайта пока собирает `--environment development` и возвращает `noindex,nofollow`. | Документация честно фиксирует safe mode и production gate; нужно отдельное решение перед индексацией. |
| P1 | `12` главных товарных PNG остаются одинаковыми для разных SKU. | Документация фиксирует это как P1/P0 для визуального доверия и не называет товарный визуальный слой полностью готовым. |
| P1 | WebMCP остается proposed/experimental Chrome-слоем. | Документация держит его как progressive enhancement и требует проверки PageSpeed Agentic Browsing после deploy. |
| P1 | В рабочем дереве есть `.DS_Store` файлы. | Это cleanup-задача рабочего дерева; текущий documentation audit только фиксирует состояние. |
| P1 | Нет постоянной команды `npm run links:audit`. | Аудит `84` содержит ручной baseline и метод; автоматизацию можно добавить отдельной задачей. |
| P2 | Доступны newer npm-версии Tailwind CSS и Netlify Database SDK. | Документация верно отражает pinned-версии; обновление требует отдельного controlled upgrade. |
| P2 | Нет импортированных Search Console данных. | Документация оставляет поля `gsc_*` пустыми до реального импорта, что правильно. |
| P2 | Часть исторических audit-файлов содержит старые оценки и старые версии стека. | Они помечены как исторические; текущие маршруты ведут к активным документам `92`, `93`, `96` и `97`. |

## 12. Оценка По 10-Балльной Шкале

| Критерий | Оценка |
|---|---:|
| Понятность для новичка | **9.8/10** |
| Русский язык и структура | **9.8/10** |
| Синхронизация с текущим проектом | **9.8/10** |
| Актуальность стека на 2026 год | **9.8/10** |
| Лучшие практики SEO/schema/CWV/WebMCP | **9.7/10** |
| Проверяемость и маршруты чтения | **9.8/10** |

Итоговая оценка: **9.8/10**.

## 13. Что Проверять После Этого Аудита

После любых новых документов:

1. Обновить [docs/01-documentation-map.md](../01-documentation-map.md).
2. Обновить `README.md` и `AGENTS.md`.
3. Проверить, что текущий полный аудит документации указывает на последний audit-снимок.
4. Запустить `git diff --check`.
5. Запустить `mise exec -- ./scripts/script_check.sh`.
6. Запустить `mise exec -- npm run build:production`.

Если меняются правила Google, Chrome, Hugo, Tailwind, Netlify, WebMCP, `llms.txt` или structured data, сначала свериться с официальными источниками, затем обновлять локальные документы.

## 14. Финальная Проверка После Правок

Финальная проверка после добавления `97` пройдена:

1. `git diff --check` — без ошибок.
2. `mise exec -- ./scripts/script_check.sh` — успешно.
3. `mise exec -- npm run build:production` — успешно.
4. HTML scan готового `public/` — `0` JSON-LD parse-ошибок и `0` битых внутренних ссылок.
5. `public/llms.txt` присутствует после сборки.

Документация синхронизирована с текущим проектом на дату 2026-07-08 после добавления WebMCP и `llms.txt`.
